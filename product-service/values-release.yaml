image:
  repository: 577201457903.dkr.ecr.ap-northeast-2.amazonaws.com/msa/product-service
  tag: 678b6a4e20decd32deb37c125d233d235fff615a
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9090

serviceMonitor:
  enabled: true

ingress:
  enabled: false

envFrom:
  - secretRef:
      name: product-service-secret

appConfig:
  spring:
    application:
      name: product-service
    datasource:
      url: jdbc:postgresql://product-db-rw.postgresql.svc.cluster.local:5432/product_db
      username: product-service
      password: ${DB_PASSWORD}
      driver-class-name: org.postgresql.Driver
    jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      hibernate:
        ddl-auto: update
      generate-ddl: true
      show-sql: true
  server:
    port: 8080
  grpc:
    server:
      port: 9090
  management:
    endpoints:
      web:
        base-path: /
        path-mapping:
          health: healthz
        exposure:
          include: health,prometheus
    endpoint:
      health:
        show-details: always
      prometheus:
        enabled: true
    tracing:
      sampling:
        probability: 1.0
    otlp:
      tracing:
        endpoint: http://tempo.monitoring.svc.cluster.local:4318/v1/traces
